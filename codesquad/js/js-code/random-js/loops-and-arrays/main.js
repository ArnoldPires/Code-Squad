// a loop that counts to 9. This includes the 0
for ( i = 0; i < 10; i++)
{
  console.log(i);
}
//Typing in alert(numbers.length); Will tell you how many objects are in the array
let numbers = ["1", "2", "3"];
alert(numbers.length);

var characterName = ["Arnold","Peter","Mark"];
